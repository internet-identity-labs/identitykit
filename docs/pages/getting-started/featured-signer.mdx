---
title: "Featured Signer"
date: "2024-06-03"
authors:
  - name: "Dan Ostrovsky"
---

import Image from "next/image"
import { useTheme } from "nextra-theme-docs"

function ThemedImage({ lightSrc, darkSrc, alt, width = 600, height = 400 }) { const { theme } =
useTheme() return ( <Image src={theme === "dark" ? darkSrc : lightSrc} alt={alt} width={width}
height={height} /> ) }

By default, IdentityKit's `featuredSigner` is set to `NFIDW` in case `signers` prop includes `NFIDW`
or was not provided (and looks like this):

<ThemedImage
  lightSrc="/img/featured-mode.png"
  darkSrc="/img/featured-mode-dark.png"
  alt="NFID IdentityKit featured mode"
/>

```jsx
import { IdentityKitProvider } from "@nfid/identitykit/react"
import { NFIDW } from "@nfid/identitykit"

export const App = () => (
  <IdentityKitProvider featuredSigner={NFIDW} {...etc}>
    {/* Your App */}
  </IdentityKitProvider>
)
```

Available options for `featuredSigner`:

- `Signer{:typescript}` - one of the signers exported by `@nfid/identitykit`, default is first of
  the signers list
- `false{:jsx}` - to disable featured signer at all

With `false{:typescript}` value provided modal will look like this:

<ThemedImage
  lightSrc="/img/compact-mode.png"
  darkSrc="/img/compact-mode-dark.png"
  alt="NFID IdentityKit compact mode"
/>

```jsx
import { IdentityKitProvider } from "@nfid/identitykit/react"

export const App = () => (
  <IdentityKitProvider featuredSigner={false} {...etc}>
    {/* Your App */}
  </IdentityKitProvider>
)
```
