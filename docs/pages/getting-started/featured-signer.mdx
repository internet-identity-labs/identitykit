---
title: "Featured Signer"
date: "2024-06-03"
authors:
  - name: "Dan Ostrovsky"
---

import ThemedImage from "../../components/ThemedImage"
import lightFeatured from "../../../public/img/featured-mode.png"
import darkFeatured from "../../../public/img/featured-mode-dark.png"
import lightCompact from "../../../public/img/compact-mode.png"
import darkCompact from "../../../public/img/compact-mode-dark.png"

By default, IdentityKit's `featuredSigner` is set to `NFIDW` in case `signers` prop includes `NFIDW`
or was not provided (and looks like this):

<ThemedImage lightSrc={lightFeatured} darkSrc={darkFeatured} alt="NFID IdentityKit featured mode" />

```jsx
import { IdentityKitProvider } from "@nfid/identitykit/react"
import { NFIDW } from "@nfid/identitykit"

export const App = () => (
  <IdentityKitProvider featuredSigner={NFIDW} {...etc}>
    {/* Your App */}
  </IdentityKitProvider>
)
```

Available options for `featuredSigner`:

- `Signer{:typescript}` - one of the signers exported by `@nfid/identitykit`, default is first of
  the signers list
- `false{:jsx}` - to disable featured signer at all

With `false{:typescript}` value provided modal will look like this:

<ThemedImage lightSrc={lightCompact} darkSrc={darkCompact} alt="NFID IdentityKit compact mode" />

```jsx
import { IdentityKitProvider } from "@nfid/identitykit/react"

export const App = () => (
  <IdentityKitProvider featuredSigner={false} {...etc}>
    {/* Your App */}
  </IdentityKitProvider>
)
```
